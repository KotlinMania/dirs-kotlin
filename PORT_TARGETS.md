# Port targets — `dirs-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `dirs` that codex-rs uses, and therefore the minimum subset that `dirs-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-43] (rank `53`, tier `direct`, status `To Do`)

**Cargo package name**: `dirs`
**Rust module name (in `use` statements)**: `dirs`

## Summary

- **1** distinct symbols imported by codex-rs
- **10** total `use` statements
- **9** codex source files affected
- **7** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `dirs::home_dir`

- **10 `use` site(s)** across **7 codex member crate(s)**: `codex-core`, `codex-core-plugins`, `codex-core-skills`, `codex-state`, `codex-tui`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core-plugins/src/marketplace.rs:10` (`codex-core-plugins`) — `use dirs::home_dir`
- `core-skills/src/loader.rs:23` (`codex-core-skills`) — `use dirs::home_dir`
- `core/src/realtime_context.rs:15` (`codex-core`) — `use dirs::home_dir`
- `state/src/bin/logs_client.rs:11` (`codex-state`) — `use dirs::home_dir`
- `tui/src/exec_command.rs:5` (`codex-tui`) — `use dirs::home_dir`
- `tui/src/history_cell.rs:3101` (`codex-tui`) — `use dirs::home_dir`
- `tui/src/markdown_render.rs:13` (`codex-tui`) — `use dirs::home_dir`
- `utils/absolute-path/src/lib.rs:1` (`codex-utils-absolute-path`) — `use dirs::home_dir`
- `utils/home-dir/src/lib.rs:2` (`codex-utils-home-dir`) — `use dirs::home_dir`
- `utils/home-dir/src/lib.rs:69` (`codex-utils-home-dir`) — `use dirs::home_dir`
